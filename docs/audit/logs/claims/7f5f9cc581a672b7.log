$ grep -n 'GNU .timeout. sends SIGTERM' .github/scripts/apt-install.sh; timeout --help | grep -c 'kill-after'
# exit=0 duration=0.006s
48:# and believed that bounded it.  It did not.  GNU `timeout` sends SIGTERM, and
1
