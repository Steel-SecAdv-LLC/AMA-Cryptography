# Development and Testing Dependencies for AMA Cryptography 2.1
# Install with: pip install -r requirements-dev.txt
#
# ALL cryptographic operations provided natively by the C library (ZERO external deps).
# Native C backend: Ed25519, AES-256-GCM, HKDF, ML-DSA-65, Kyber-1024, SPHINCS+.
# Build with: cmake -B build -DAMA_USE_NATIVE_PQC=ON && cmake --build build

# Build dependencies (required for Cython extensions)
Cython>=3.0.0

# NumPy: required for monitoring/equations modules
numpy>=1.24.0

# SciPy: required for monitoring/equations modules (1.11+ requires Python 3.9+)
scipy>=1.11.0

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-timeout>=2.1.0
pytest-benchmark>=4.0.0
pytest-xdist>=3.0.0
hypothesis>=6.0.0

# Code Quality
black>=26.3.1; python_version >= "3.10"
ruff>=0.4.0
mypy>=1.9.0

# Security Scanning
bandit>=1.7.0
# pip-audit (installed in CI) provides dependency vulnerability scanning via OSV.

# Benchmarking (optional — benchmark_suite.py uses psutil when available)
# psutil>=5.9.0

# Build and packaging
setuptools>=61.0
wheel>=0.40.0
build>=0.10.0
